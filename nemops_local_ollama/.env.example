# Nemotron model — auto-set by setup.sh based on your RAM
# 16GB Mac: nemotron-3-nano:30b-cloud (free Ollama cloud)
# 32GB+ Mac: nemotron-3-nano:30b (fully local)
NEMOTRON_MODEL=nemotron-3-nano:30b-cloud

# Ollama server
OLLAMA_HOST=http://localhost:11434

# GPU metrics mode: "mock" (default, no GPU needed) or "real" (requires NVIDIA GPU + pynvml)
GPU_MODE=mock

# ChromaDB persistence
CHROMA_PATH=./data/chromadb

# Agent config
AGENT_MAX_STEPS=10
AGENT_VERBOSE=true
