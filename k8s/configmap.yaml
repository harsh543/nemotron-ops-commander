apiVersion: v1
kind: ConfigMap
metadata:
  name: nemotron-config
data:
  APP_ENV: "production"
  APP_NAME: "nemotron-ops-commander"
  LOG_LEVEL: "INFO"
  API_HOST: "0.0.0.0"
  API_PORT: "8000"
  CHROMA_PERSIST_DIR: "/app/chroma_storage"
  CHROMA_COLLECTION: "incidents"
  PROMETHEUS_PORT: "9000"
