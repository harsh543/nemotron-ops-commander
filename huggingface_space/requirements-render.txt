# Render Web Service build — fully explicit, deliberately independent
# from requirements.txt (which may assume HF-Space-platform-managed
# packages like gradio/torch/spaces and is not safe to build on Render).
# Keep this file in sync manually if huggingface_space/*.py's imports change.

torch
transformers>=4.37.0
accelerate>=0.26.0

chromadb>=0.4.22
sentence-transformers>=2.3.0  # Supports NVIDIA llama-nemotron-embed-1b-v2

gradio>=4.15.0
huggingface-hub>=0.20.0,<1.0
openai>=1.30.0  # Nebius Token Factory (OpenAI-compatible)
requests>=2.31.0  # RevenueCat REST API (entitlement checks)
render>=1.0.1  # Render Workflows SDK (reindex_workflow.py)

pydantic>=2.5.0
numpy>=1.24.0
