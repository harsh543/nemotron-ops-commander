# Nemotron-Ops-Commander — HuggingFace Space (T4 GPU)
# Local GPU inference + RAG pipeline

# LLM inference (local GPU)
torch
transformers>=4.37.0
accelerate>=0.26.0

# RAG & embeddings
chromadb>=0.4.22
sentence-transformers>=2.3.0  # Supports NVIDIA llama-nemotron-embed-1b-v2

# UI & API
gradio>=4.15.0
huggingface-hub>=0.20.0,<1.0
openai>=1.30.0  # Nebius Token Factory (OpenAI-compatible)
requests>=2.31.0  # RevenueCat REST API (entitlement checks)

# Data
pydantic>=2.5.0
numpy>=1.24.0
